apiVersion: v1
kind: Service
metadata:
  labels:
  name: foodorderingsystem
spec:
  ports:
    - name: foodorderingsystem
      port: 8080
      targetPort: 80808080
      protocol: TCP
  selector:
    app: foodorderingsystem
  type: NodePort
